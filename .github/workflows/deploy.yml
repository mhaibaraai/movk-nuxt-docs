name: Deploy Docs

on:
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/${{ github.repository }}/docs

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: pnpm

      - name: Restore Nuxt build cache
        uses: actions/cache@v5
        with:
          path: |
            docs/.nuxt
            layer/.nuxt
            node_modules/.cache
          key: nuxt-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}-${{ hashFiles('docs/content/**') }}
          restore-keys: |
            nuxt-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}-
            nuxt-${{ runner.os }}-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Standard GitHub runners only have ~7GB RAM; the Nuxt prerender peaks
      # above that, so provision swap to let V8 use the full heap without thrashing.
      - name: Provision swap space
        run: |
          sudo swapoff -a || true
          sudo rm -f /mnt/swapfile
          sudo fallocate -l 12G /mnt/swapfile
          sudo chmod 600 /mnt/swapfile
          sudo mkswap /mnt/swapfile
          sudo swapon /mnt/swapfile
          sudo sysctl -w vm.swappiness=80
          free -h

      - name: Build docs site
        env:
          NODE_OPTIONS: --max-old-space-size=10240
          NUXT_GITHUB_TOKEN: ${{ secrets.NUXT_GITHUB_TOKEN }}
          ALIBABA_API_KEY: ${{ secrets.ALIBABA_API_KEY }}
        run: pnpm build

      - uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
