---
name: review-docs
description: |
  审查文档的质量、清晰度、SEO 和技术正确性。
  针对 Docus/Nuxt Content 优化，但与任何 Markdown 文档兼容。
  当被要求时使用：
  "审查文档"、"检查文档"、"审计文档"、
  "验证文档"、"改进文档质量"、"分析文档",
  "检查文档"、"审查文档页面"、"验证 MDC 语法",
  "检查 SEO 问题"、"分析文档结构"。
  提供按优先级分类的可操作建议（严重、重要、不错）。
---

# 审查文档

全面的文档审查，针对 Docus/Nuxt Content 进行了优化，但与任何 Markdown 文档兼容。

## 工作流概览

本技能执行 5 步审查过程：

1. **检测项目类型** - 识别 Docus/Nuxt Content 还是通用 Markdown
2. **分析结构** - 映射文档组织、语言地区、部分
3. **技术验证** - 检查 frontmatter、MDC 语法（如适用）、文件命名
4. **内容质量审查** - 评估清晰度、SEO、结构、i18n
5. **生成报告** - 提供分类的、可操作的建议

### 优先级级别

- **严重** - 阻止部署或导致错误（缺少 frontmatter、无效的 MDC 语法）
- **重要** - 显著影响 UX/SEO（差的元数据、被动语态、不清晰的标题）
- **不错** - 抛光和优化建议（添加标注、改进示例）

### 期望

本技能仅生成**详细的报告**。审查后，如果请求，它会提供修复已确定问题的帮助。

---

## 步骤 1：检测项目类型

**目标：** 确定这是 Docus/Nuxt Content 项目还是通用 Markdown 文档。

### 检测指示器

**检查 Docus/Nuxt Content：**
1. **package.json 依赖项：**
   - `"docus"` - Docus 主题
   - `"@nuxt/content"` - Nuxt Content 模块
   - `"@nuxtjs/mdc"` - MDC 支持

2. **配置文件：**
   - `nuxt.config.ts` 或 `nuxt.config.js` 带有 `@nuxt/content` 模块
   - `content.config.ts` - 内容集合配置

3. **内容结构：**
   - `content/` 或 `docs/content/` 目录
   - 子目录中的 `.navigation.yml` 文件
   - markdown 文件中的 MDC 语法（`::component-name`）

4. **项目结构：**
   - 编号目录（`1.getting-started/`、`2.guide/`）
   - 带有 `navigation`、`seo` 字段的 Frontmatter

### 项目类型分类

**类型 A：Docus/Nuxt Content 项目**
- 所有 Docus 特定的验证都适用
- MDC 组件语法检查（u- 前缀要求）
- Nuxt Content frontmatter 结构
- 导航文件（.navigation.yml）
- 完整的技术验证

**类型 B：通用 Markdown 文档**
- 仅限基础 Markdown 验证
- 通用 frontmatter（title、description、date、author）
- 标准 Markdown 语法
- 专注内容质量（SEO、清晰度、结构）
- 无 Docus 特定的技术检查

### 检测输出

检测后，在报告中注明：
```
项目类型：[Docus/Nuxt Content | 通用 Markdown]
验证模式：[完整（Docus 特定）| 基础（仅 Markdown）]
```

**根据检测的类型调整验证步骤：**
- **类型 A（Docus）：** 执行所有步骤并进行完整验证
- **类型 B（通用）：** 跳过 Docus 特定的检查，专注于内容质量

---

## 步骤 2：分析文档结构

### 定位内容目录

找到文档内容目录：
- 检查 `docs/content/`（最常见）
- 检查 `content/`（根级别）
- 检查 `app/content/`（替代位置）

### 检测语言地区

通过检查子目录来确定语言结构：

**单一语言**（无语言地区子目录）：
```
content/
├── index.md
├── 1.getting-started/
└── 2.guide/
```

**多语言**（语言地区子目录）：
```
content/
├── en/
│   ├── index.md
│   ├── 1.getting-started/
│   └── 2.guide/
└── fr/
    ├── index.md
    ├── 1.getting-started/
    └── 2.guide/
```

**检测逻辑：**
- 如果立即子目录是 2 字母代码（en、fr、es、de 等），则是多语言
- 如果立即子目录是编号的（1.getting-started），则是单一语言

### 列出文档部分

识别每个语言地区内的所有编号目录：
- `1.getting-started/`
- `2.guide/` 或 `2.concepts/`
- `3.api/` 或 `3.essentials/`
- `4.advanced/` 或 `4.ai/`

对每个部分，注明：
- 部分名称
- `.navigation.yml` 文件的存在
- 页面数量（计数 `.md` 文件）
- 页面文件名称

### 验证核心文件

检查所需文件：
- [ ] 在每个语言地区的根目录中存在 `index.md`
- [ ] 在每个部分目录中存在 `.navigation.yml`
- [ ] 编号文件遵循模式（`1.introduction.md`、`2.installation.md`）

### 创建结构图

为报告记录结构：
```
项目：[project-name]
语言地区：[en, fr]（或"单一语言"）
部分：
  - 1.getting-started：5 个页面，.navigation.yml ✅
  - 2.guide：8 个页面，.navigation.yml ✅
  - 3.api：3 个页面，.navigation.yml ❌（缺失）
```

---

## 步骤 3：技术验证

**根据步骤 1 中检测的项目类型调整验证。**

### 针对 Docus/Nuxt Content 项目（类型 A）

使用 [references/technical-checks.md](../references/technical-checks.md) 进行完整的技术验证：

**验证：**
1. **Frontmatter 结构** - 必需：`title`、`description`。可选：`navigation`、`seo`、`links`
2. **MDC 组件语法** - 所有 Nuxt UI 组件必须有 `u-` 前缀（`::u-page-hero`、`:::u-button`）
3. **代码块标签** - 代表文件的所有代码块需要描述性标签（` ```vue [App.vue] `、` ```ts [config.ts] `）
4. **代码语言一致性** - 代码示例应匹配项目的语言栈（例如，如果项目使用 TypeScript，`:lang="ts"` 在 Vue `<script setup>`）
5. **包管理器覆盖** - `::code-group` 安装块必须覆盖项目/生态系统支持的所有包管理器
6. **代码预览** - 使用 `::code-preview` 用于视觉上可呈现的示例（表格、列表、呈现的 markdown 等）
7. **代码组范围** - 仅组合等效替有品是（例如包管理器、框架变体）——不要混合无关的步骤（例如安装命令 + 配置文件）
8. **文件命名** - 编号目录/文件、kebab-case、每个部分中的 `.navigation.yml`
9. **隐藏页面** - 为不应出现在边栏但应作为路由存在的页面使用 `navigation: false`

**常见严重错误：**
- 缺少 `u-` 前缀：`::page-hero` → 应该是 `::u-page-hero`
- 缺少必需的 frontmatter：`title`、`description`
- 无效的 `.navigation.yml` 结构
- 缺少部分 `index.md` 导致部分根 URL 上的 404

见 [references/technical-checks.md](../references/technical-checks.md) 获取完整的验证规则、示例和错误模式。

### 针对通用 Markdown 项目（类型 B）

**简化的验证** - 跳过 Docus 特定的检查：

**基础 Frontmatter 验证：**
- 检查常见字段：`title`、`description`、`date`、`author`、`tags`
- 无严格要求 - 只是建议
- 如果完全缺失 frontmatter，则标记

**标准 Markdown 语法：**
- 验证基础 markdown（标题、列表、链接、代码块）
- 检查损坏的内部链接
- 验证图像路径存在

**跳过：**
- MDC 组件语法（不适用）
- Nuxt Content frontmatter 结构
- `.navigation.yml` 文件
- Docus 特定的约定

**重点：**
- 内容质量（下一步）
- SEO 优化
- 清晰度和可读性
- 一般结构

---

## 步骤 4：内容质量审查

**此步骤适用于所有项目类型**（Docus 和通用 Markdown）。

根据四个维度评估内容质量。有关详细的清单，请参考参考文件。

### 清晰度审查

使用 [references/clarity-checks.md](../references/clarity-checks.md) 检查：
- **语气与声调：** 主动语态、现在时、第二人称
- **句子结构：** 目标 15-20 字，避免冗长短语
- **段落结构：** 2-5 句，200-400 字在标题之间
- **基于动作的标题：** 页面标题（H1）和标题（H2/H3）在指南中使用动作动词（Nuxt 模式）
  - 示例："创建你的第一个模块"、"配置你的应用"、"构建插件"
  - 例外：开始使用（名词）、API（函数名）、概念（描述性）
- **术语** - 一致的命名、定义的技术术语
- **代码示例：** 完整、可复制粘贴、逼真、带文件标签

### SEO 审查

使用 [references/seo-checks.md](../references/seo-checks.md) 检查：
- **标题：** 50-60 字符、关键字、独特
- **描述：** 120-160 字符、引人注目、独特
- **标题：** 单一 H1、逻辑层级（H1→H2→H3）、描述性
- **URL：** Kebab-case、描述性、稳定
- **链接：** 描述性锚点、"后续步骤"部分
- **内容长度：** 登陆 300+ 字、指南 400+ 字、200-400 字在部分之间
- **图像：** Alt 文本、颜色模式变体

### 结构审查

使用 [references/structure-checks.md](../references/structure-checks.md) 检查：
- **层级：** 最多 3 级、逻辑进展
- **组织：** 2-15 页/部分、`.navigation.yml` 存在、合适的图标
- **流程：** 逻辑进展、"后续步骤"链接、无孤立页面
- **登陆页：** 英雄、功能、快速开始
- **一致性：** 页面之间的相似结构

### i18n 审查（如果是多语言）

使用 [references/i18n-checks.md](../references/i18n-checks.md) 检查：
- **并行结构：** 语言地区之间相同的目录、文件、页面数
- **翻译完整性：** 相似内容长度（±30%）、相同标题
- **导航：** 相同图标、翻译的标题
- **语言地区特定：** 无混合语言、正确的内部链接、翻译的注释

---

## 步骤 5：生成报告

使用 [assets/report-template.md](assets/report-template.md) 创建全面的审查报告。

**根据项目类型调整报告：**
- **Docus/Nuxt Content：** 包含所有部分（技术、SEO、清晰度、结构、i18n）
- **通用 Markdown：** 专注于内容质量（SEO、清晰度、结构），省略 Docus 特定的技术问题

### 报告结构

```markdown
# 文档审查报告

**生成时间：** [当前日期和时间]
**项目：** [来自 package.json 或目录的项目名称]
**已审查：** 跨 [Y] 部分在 [语言地区] 中的 [X] 页面

---

## 执行摘要

- **严重问题：** [计数]（必须修复 - 阻止部署/导致错误）
- **重要问题：** [计数]（对 UX/SEO 的显著影响）
- **不错的建议：** [计数]（抛光和优化建议）

**总体评估：** [1-2 句话总结文档质量]

---

## 严重问题

[按类别分组的所有严重问题列表]

### 技术：MDC 语法错误

#### Nuxt UI 组件缺少 u- 前缀

**文件：** `/content/en/1.getting-started/1.introduction.md:15`

**问题：** 页面英雄组件缺少 `u-` 前缀

**当前：**
\`\`\`markdown
::page-hero
#title
欢迎
::
\`\`\`

**应该是：**
\`\`\`markdown
::u-page-hero
#title
欢迎
::
\`\`\`

**影响：** 组件将不会呈现，导致构建错误

---

### 技术：缺少 Frontmatter

[每个问题的类似格式]

---

## 重要问题

[按类别分组的所有重要问题列表：SEO、清晰度、结构]

### SEO：次优元数据

[文件路径和建议的详细信息]

### 清晰度：被动语态

[示例和建议的改写詳細信息]

### 结构：糟糕的导航

[組織建议的詳細信息]

---

## 不错的建议

[按类别列出的优化建议]

### SEO 优化
- **[文件]**: [建议]

### 清晰度改进
- **[文件]**: 考虑为 [特定内容] 添加 `::tip` 标注

### 结构增强
- **[部分]**: 考虑分割成小节

---

## 语言地区特定问题

[仅在检测到多语言时]

### 法语（`/fr/`）
- [翻译问题]

---

## 统计

### 内容概览

| 部分 | 页面（en） | 页面（fr） | 平均字数/页 |
|---------|------------|------------|----------------|
| 开始使用 | [X] | [X] | ~[XXX] |
| 指南 | [X] | [X] | ~[XXX] |

### 问题分析

| 类别 | 严重 | 重要 | 不错 | 总计 |
|----------|------|---------|------|-------|
| 技术 | [X] | [X] | [X] | [X] |
| SEO | [X] | [X] | [X] | [X] |
| 清晰度 | [X] | [X] | [X] | [X] |
| 结构 | [X] | [X] | [X] | [X] |
| i18n | [X] | [X] | [X] | [X] |
| **总计** | **[X]** | **[X]** | **[X]** | **[X]** |

---

## 正面亮点

[指出做得很好的 2-3 个地方]
- 很好地使用标注和代码示例
- 一致的 MDC 组件使用
- 组织良好的部分结构

---

## 推荐操作计划

### 优先级 1：修复严重问题（今天）
1. [具体可操作的项目]

**估计修复：** [X] 文件

### 优先级 2：重要问题（本周）
1. [具体可操作的项目]

**估计修复：** [X] 文件

### 优先级 3：不错的建议（下一个冲刺）
1. [具体可操作的项目]

**估计修复：** [X] 文件

---

## 后续步骤

**你想要我：**

1. **修复所有严重问题** - 我可以自动纠正 MDC 语法和 frontmatter 问题
2. **重写特定部分** - 指出哪些页面需要清晰度改进，我会重写
3. **优化 SEO 元数据** - 我可以更新所有标题和描述为最优长度
4. **重组内容** - 如果部分需要重新组织，我可以帮助重组
5. **完成翻译** - 如果你需要 i18n 内容完成

**或指定你想首先关注什么。**
```

### 报告生成指南

**要具体：**
- 包括确切的文件路径和行号
- 显示当前 vs 推荐代码
- 解释每个问题为什么重要（影响）

**要可操作：**
- 提供清晰的修复说明
- 包括代码示例
- 按影响排定优先级

**要平衡：**
- 突出正面方面
- 不要用次要问题压垮
- 专注于高影响力的改进

**生成报告后：**
- 如果用户请求，提供修复问题的帮助
- 准备好处理特定的类别或文件
- 建议从严重问题开始

---

## 快速参考

**最常见的问题：**
- Nuxt UI 组件上缺少 `u-` 前缀（`::page-hero` → `::u-page-hero`）
- SEO 描述太短（需要 120-160 字)
- 说明中的被动语态（"可以完成" → "完成")
- 泛型标题（"配置" → "配置你的应用"）
- 代码块缺少文件名标签（代表文件的每个块都应该有一个）
- 代码语言与项目栈不匹配（例如，缺少 `lang="ts"` 在 Vue `<script setup>` 在 TypeScript 项目中）
- 在 `::code-group` 安装块中不完整的包管理器覆盖（根据生态系统/项目检查）
- 无关步骤分组在 `::code-group` 中（例如，安装命令 + 配置文件）—— 保持为单独的块
- `::code-preview` 缺失，其中呈现预览会增加清晰度（表格、列表等）
- 缺少部分登陆页 → 部分根 URL 上的 404（如果需要，添加带有 `navigation: false` 的 `index.md`）

**完整的清单和示例见参考文件。**
